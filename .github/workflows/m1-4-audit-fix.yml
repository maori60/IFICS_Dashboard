name: M1.4 Lockfile Sync

on:
  push:
    branches:
      - milestone-1/foundation
    paths:
      - 'package.json'
      - '.github/workflows/m1-4-audit-fix.yml'

permissions:
  contents: write

jobs:
  sync-lockfile:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      DATABASE_URL: postgresql://audit:audit@127.0.0.1:5432/audit?schema=public

    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: milestone-1/foundation

      - name: Use Node 22
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - name: Use npm 11
        run: npm install --global npm@11

      - name: Regenerate lockfile from declared versions
        run: npm install --package-lock-only --ignore-scripts

      - name: Verify no critical production vulnerabilities remain
        run: npm audit --omit=dev --audit-level=critical

      - name: Commit synchronized lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          if git diff --cached --quiet; then
            echo "package-lock.json is already synchronized."
            exit 0
          fi
          git commit -m "fix(m1.4): synchronize remediated dependency lock"
          git push origin HEAD:milestone-1/foundation
